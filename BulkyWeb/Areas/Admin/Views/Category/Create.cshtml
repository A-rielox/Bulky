@model Category

@* si no mando el obj<Category> desde el controller, aca lo va a crear
	con valores default para mandarlo de vuelta *@

<div class="card shadow border-0 mt-4">

	<div class="card-header bg-secondary bg-gradient ml-0 py-3">
		<div class="row">
			<div class="col-12 text-center">
				<h2 class="text-white py-2">Create Category</h2>
			</div>
		</div>
	</div>

	<div class="card-body p-4">
		<form method="post" class="row">
			@* ** *@
			<div class="border p-3">

				<div asp-validation-summary="All"></div> @* *** *@

				<div class="mb-3">
					<label asp-for="Name"></label>
					<input asp-for="Name" class="form-control" /> @* * *@
					<span asp-validation-for="Name" class="text-danger"></span>
				</div>

				<div class="mb-3">
					<label asp-for="DisplayOrder"></label>
					<input asp-for="DisplayOrder" class="form-control" />
					<span asp-validation-for="DisplayOrder" class="text-danger"></span>
				</div>


				<div class="row">
					<div class="col-6 col-md-3">
						<button type="submit" class="btn btn-primary form-control">Create</button>
					</div>

					<div class="col-6 col-md-3">
						<a asp-controller="Category" asp-action="Index" class="btn btn-outline-secondary form-control">
							Back to List
						</a>
					</div>
				</div>
			</div>
		</form>
	</div>
</div>
@* en seccion scripts solo por ser script, si no, se puede poner directo en el html,
	en caso de serlo *@
@* las validaciones custom las hace en el server, las del modelo Category, las va a
	hacer en el client ( las q pongo con annotations y eso ) *@
@section scripts{
	@{
		<partial name="_ValidationScriptsPartial" />
	}
}

@* * *@
@* asp asp-for="Name" va a hacer el bind entre la prop del modelo
	Category y el valor del input tabien no va a nesecitar el type="text"
	pq va a agarrarlo de la definicion de la clase Category *@

@* ** *@
@* como la form tiene el metodo post => al dar el submit le va a dar al mismo endpoint
	que me trajo aca ( el de create ) pero p' post request *@

@* *** *@
@* <div asp-validation-summary="ModelOnly"> para que ponga solo las validaciones del
	modelo gral, osea, las q no estan asignadas a una prop como name o display order,
	en este caso la de name != "test" *@